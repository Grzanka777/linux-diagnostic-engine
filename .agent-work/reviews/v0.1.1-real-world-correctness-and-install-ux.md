# LDE v0.1.1 Real-World Correctness + Install UX Review

Date: 2026-08-29
Repository: `<REDACTED-PATH>`
Baseline: `v0.1.0` / `b7fa30b`
Reviewed scope: systemd duration and BOOT-SLOW correctness, recommendation and restriction rendering, supported installation, and report output UX.

## Verdict

`PASS` for the requested v0.1.1 scope.

The implementation preserves the existing diagnostic surface and compatibility contracts while making the requested real-machine and installation behavior observable and testable. No commit, push, tag, service mutation, shell startup-file mutation, Git state mutation, or NeuralEngine state write was performed.

## Contract checks

| Contract | Result | Evidence |
| --- | --- | --- |
| Product version | PASS | `0.1.1` in `pyproject.toml`, runtime `lde --version`, and candidate wheel metadata |
| Report compatibility | PASS | Report metadata remains `2.1.0` |
| Snapshot schema | PASS | Installed default and explicit runs produced schema `3` snapshots |
| No new diagnostics | PASS | Existing `BOOT-SLOW-001` and existing rule/finding IDs are reused |
| Systemd duration parsing | PASS | Compound `1min 31.345s` and target duration parse correctly; malformed expressions are rejected |
| BOOT-SLOW semantics | PASS | `graphical.target` is preferred when available, userspace is the fallback; the real host's `91.345s` userspace plus `4.819s` target does not produce a false slow-boot finding |
| Recommendation deduplication | PASS | Candidate IDs are deduplicated and rendered in non-overlapping priority groups |
| Restriction scoping | PASS | Btrfs restrictions attach only to Btrfs findings; dmesg restrictions attach only to kernel-taint findings |
| Default reports directory | PASS | Runtime and installer use `$XDG_DATA_HOME/lde/reports`, falling back to `~/.local/share/lde/reports` |
| Explicit output override | PASS | `--output-dir` remains authoritative |
| Exact report-path summary | PASS | Successful default and explicit runs print the resolved absolute generated report path; missing/unreadable report and run failures do not print success paths |
| Destination safety | PASS | Existing compare destination is rejected with exit code `1` and remains unchanged |
| Read-only boundary | PASS | Generated report command section contains no mutating service, package, filesystem, or destructive commands |

## Implemented changes

- `constants.py`: product version `0.1.1` and runtime XDG/default report-directory resolution.
- `syscheck.py`: systemd duration parser, target-aware boot measurement, recommendation deduplication/non-overlapping rendering, scoped restrictions, runtime default output resolution, and verified absolute report-path summary.
- `diagnostic_rules.py`: BOOT-SLOW evaluation follows the same target-first/userspace-fallback semantics.
- `install.sh`: supported no-sudo `uv tool install --force` flow; creates the reports directory immediately and prints installed version and path. It does not edit shell rc files or system services.
- `pyproject.toml` and `README.md`: v0.1.1 packaging and installation/output documentation.
- `test_syscheck.py` and `test_packaging.py`: focused regression and packaging/installer coverage.

## Fresh validation evidence

### Repository checks

```text
PYTHONDONTWRITEBYTECODE=1 TMPDIR=/tmp python3 -m pytest --collect-only -q
881 tests collected

PYTHONDONTWRITEBYTECODE=1 TMPDIR=/tmp python3 -m pytest -q
881 passed in 8.53s

ruff format --check constants.py syscheck.py diagnostic_rules.py test_syscheck.py test_packaging.py
5 files already formatted

ruff check .
All checks passed!

git diff --check
PASS
```

Focused correctness tests passed, including `68 passed` for the initial systemd/boot/recommendation selection and `35 passed` for the final recommendation-focused selection. Runtime version verification returned:

```text
Linux Diagnostic Engine 0.1.1
```

### Candidate wheel

Candidate wheel:
`/tmp/lde-v011-candidate-final2.8n3DSy/wheel/linux_diagnostic_engine-0.1.1-py3-none-any.whl`

The wheel contract passed: version `0.1.1`, Python requirement `>=3.10`, no runtime dependencies, `lde = syscheck:main`, compatibility code retained, and no `.codex`, `.agent-work`, or test files included.

### Isolated installer proof

`install.sh` was executed from an isolated temporary HOME with the repository's `uv tool install` flow:

- with `XDG_DATA_HOME`, it created and reported `$XDG_DATA_HOME/lde/reports`;
- without `XDG_DATA_HOME`, it created and reported `$HOME/.local/share/lde/reports`;
- both runs printed `lde --version: Linux Diagnostic Engine 0.1.1`;
- both installed tools were removed with `uv tool uninstall` after the proof;
- no `sudo`, `systemctl`, shell rc update, Git command, or NeuralEngine write was performed.

### Installed real-machine proof

The candidate wheel was installed into an isolated temporary virtual environment outside the repository. On the current host, `systemd-analyze` reported:

```text
1min 31.345s (userspace) = 1min 47.963s
graphical.target reached after 4.819s in userspace.
```

The installed `lde run` produced both reports successfully:

- default: `/tmp/lde-v011-runtime-final2.2wyK0f/xdg-data/lde/reports/lde-g777-20260829-220819.md`
- explicit: `/tmp/lde-v011-runtime-final2.2wyK0f/explicit-reports/lde-g777-20260829-220821.md`

For both runs, the final summary exactly matched the resolved existing report path. Independent validation confirmed schema `3`, report compatibility `2.1.0`, resolvable observation/evidence/finding lineage, no duplicate finding IDs in recommendation output, no unrelated restriction leakage, and no traceback.

## Protected-state check

The pre-existing untracked `.codex/`, `build/`, and `linux_diagnostic_engine.egg-info/` paths remain unmodified and were not used as change targets. `neural status` was read-only; it reported the existing `BINDING_MISSING` state, and no Brain record or state file was changed.

## Remaining limitations

- Real-machine verification covers the current host and the supported local Python environment; a separate Python 3.10 runtime was not provisioned.
- The installer assumes `uv` is already installed, as required by the supported installation flow.
- No release/tag action was performed by design.

## Gate markers

```text
BOOT_DURATION_PARSING_CORRECT = YES
BOOT_SLOW_SEMANTICS_CORRECT = YES
RECOMMENDATION_DEDUPLICATION_CORRECT = YES
RESTRICTION_SCOPING_CORRECT = YES
DEFAULT_REPORT_DIRECTORY_PROVISIONED = YES
DEFAULT_REPORT_OUTPUT_USABLE = YES
INSTALLER_NO_SUDO = YES
XDG_REPORT_PATH_CORRECT = YES
EXPLICIT_OUTPUT_DIR_OVERRIDE_PRESERVED = YES
REPORT_COMPATIBILITY_2_1_0 = YES
SNAPSHOT_SCHEMA_3 = YES
NO_NEW_DIAGNOSTICS = YES
READ_ONLY_BOUNDARY = YES
CANDIDATE_WHEEL_VALIDATED = YES
```
