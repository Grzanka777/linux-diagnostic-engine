# LDE v0.1.1 Independent Release-Readiness Audit

Date: 2026-08-29
Repository: `<REDACTED-PATH>`
Baseline: `v0.1.0^{commit} = b7fa30b9ca71fc8bdd11fc26d1cd63a9993176b3`
Audited state: `HEAD = origin/master = b7fa30b9ca71fc8bdd11fc26d1cd63a9993176b3` plus the uncommitted v0.1.1 implementation under review.

## Final verdict

```text
V0_1_1_RELEASE_AUDIT = PASS
RELEASE_BLOCKERS = 0
HIGH_ISSUES = 0
READY_TO_COMMIT_V0_1_1 = YES
READY_TO_TAG_V0_1_1_AFTER_COMMIT = YES
FEATURE_EXPANSION_REMAINS_FROZEN = YES
```

No material release-readiness finding was identified. The audit was read-only with respect to production, tests, configuration, services, Git history, and NeuralEngine state. The requested audit report is the only new review artifact created in the repository during this audit; `.codex/` was not touched.

## Scope and change review

The full tracked delta from the supplied baseline is limited to:

```text
README.md
constants.py
diagnostic_rules.py
pyproject.toml
syscheck.py
test_packaging.py
test_syscheck.py
```

The accepted implementation also contains the expected untracked `install.sh`. The pre-existing untracked `.codex/`, `build/`, and `linux_diagnostic_engine.egg-info/` paths were preserved. No new diagnostic kind, rule ID, or raw diagnostic source ID was introduced by the v0.1.1 delta; the existing `BOOT-SLOW-001` contract is reused.

## Requirement matrix

| Requirement / risk | Independent evidence | Result |
| --- | --- | --- |
| Compound systemd durations | Direct parser assertions for `1min 31.345s`, `250ms`, `1h 2min 3.5s`, and malformed values; patched `collect_systemd` execution | PASS |
| Target-first BOOT-SLOW | Real-shaped `systemd-analyze` sample with `91.345s` userspace and `4.819s` target produced no slow finding; slow `31.2s` target produced `BOOT-SLOW-001` with `measurement_source=graphical.target`; userspace fallback also passed | PASS |
| Recommendation deduplication | Independent duplicate-finding generation and Markdown rendering assertions; installed report inspection found unique recommendation IDs | PASS |
| Restriction scoping | Independent findings for systemd, Btrfs, and kernel taint proved only relevant restrictions attach | PASS |
| Public product version | Source/manifest, wheel metadata, direct CLI, and installed CLI report `0.1.1` | PASS |
| Report compatibility and snapshot schema | Installed reports retain compatibility `2.1.0`; both installed snapshots use schema `3` | PASS |
| Supported installer | Isolated XDG and HOME-fallback runs created the expected reports directory, printed version/path, and installed via `uv tool install`; no sudo/systemd/shell-rc mutation | PASS |
| Default and explicit output paths | Installed real-machine runs wrote to `$XDG_DATA_HOME/lde/reports` by default and honored explicit `--output-dir` | PASS |
| Exact report-path summary | Both successful installed runs printed exactly the resolved absolute generated report path; live snapshot-collision failure exited `1` without a success path | PASS |
| Destination safety | Existing compare destination remained `keep` after a clean exit-1 rejection; repository collision tests also cover file and symlink targets | PASS |
| Read-only/privacy boundary | `run_cmd` uses argument vectors without shell execution; runtime report omitted a sentinel secret and mutating command forms; no shell startup files were created | PASS |
| Python support | Manifest declares `>=3.10`, runtime dependencies are empty, and current modules parse under Python 3.10 grammar; Python 3.14.7 was the only installed interpreter | PASS with environment limitation |
| Git safety | `git diff --cached --quiet` passed; no Git mutation command was used; HEAD and origin remained at the supplied baseline | PASS |

## Fresh validation evidence

### Repository checks

```text
PYTHONDONTWRITEBYTECODE=1 TMPDIR=/tmp python3 -m pytest -q
881 passed in 8.50s

ruff check .
All checks passed!

ruff format --check .
8 files already formatted

git diff --check
PASS
git diff --cached --quiet
PASS
```

Independent behavioral checks returned:

```text
INDEPENDENT_BEHAVIORAL_AUDIT=PASS
BOOT_DURATION_AND_TARGET_SEMANTICS=PASS
RECOMMENDATION_AND_RESTRICTION_SEMANTICS=PASS
VERSION_COMPATIBILITY=PASS
```

The current host's direct systemd evidence was:

```text
Startup finished in 14.425s (firmware) + 8.130s (loader) + 2.906s (kernel) +
5.034s (initrd) + 1min 31.345s (userspace) = 1min 47.963s
graphical.target reached after 4.819s in userspace.
```

### Candidate wheel and reproducibility

Two clean source exports were built with the same `SOURCE_DATE_EPOCH`, using offline/no-build-isolation wheel builds. Both produced the same SHA-256:

```text
Wheel: /tmp/lde-v011-wheel-audit.XTvfgn/wheel-one/linux_diagnostic_engine-0.1.1-py3-none-any.whl
SHA-256: e339b26a1a50e16b0ec1601b7c7eec87c3f04cb0fd2263d5bb308a47726a1e9a
WHEEL_REPRODUCIBLE = PASS
```

Wheel contents were exactly nine distribution/module/license metadata files, including `constants.py`, `diagnostic_rules.py`, `syscheck.py`, `METADATA`, `WHEEL`, entry points, and `RECORD`. It contains no tests, `.codex/`, or `.agent-work/`. Metadata confirms:

```text
Version: 0.1.1
Requires-Python: >=3.10
Dependencies: none
Entry point: lde = syscheck:main
License: MIT
```

### Isolated installer

The supported installer was executed from a clean temporary checkout at:
`/tmp/lde-v011-installer-audit-rerun.Eiq9R0/source`.

```text
XDG_REPORT_PATH_CORRECT = PASS
HOME_FALLBACK_REPORT_PATH_CORRECT = PASS
INSTALLER_ISOLATED_PROOF = PASS
```

The XDG run created and printed:
`$XDG_DATA_HOME/lde/reports`.

The fallback run created and printed:
`$HOME/.local/share/lde/reports`.

Both runs printed `lde --version: Linux Diagnostic Engine 0.1.1`, used `uv tool install`, and were removed from their isolated tool directories after verification. No shell startup file, system service, sudo path, Git state, or NeuralEngine state was modified.

### Installed real-machine workflow

The reproducible candidate wheel was installed into a temporary virtual environment outside the repository:
`/tmp/lde-v011-runtime-audit.L13DnS/venv`.

The installed CLI produced:

```text
DEFAULT_REPORT=/tmp/lde-v011-runtime-audit.L13DnS/xdg-data/lde/reports/lde-g777-20260829-222157.md
EXPLICIT_REPORT=/tmp/lde-v011-runtime-audit.L13DnS/explicit-reports/lde-g777-20260829-222159.md
EXACT_ABSOLUTE_SUMMARIES=PASS
SCHEMA3_COMPATIBILITY_LINEAGE=PASS
PRIVACY_READ_ONLY_RUNTIME_BOUNDARY=PASS
DESTINATION_SAFETY=PASS
```

Independent validation confirmed for both snapshots and reports:

- schema version is `3`;
- `metadata.syscheck_version` is `2.1.0`;
- evidence, observation, finding, raw-diagnostic, and recommendation lineage references resolve;
- recommendation IDs are unique and systemd recommendations have no unrelated blockers;
- the real report contains the compound userspace duration and target line, without a false slow-boot finding;
- the sentinel `LDE_AUDIT_SECRET` and its value are absent;
- no shell startup files were created.

A live run with a pre-existing snapshot destination exited with code `1`, preserved the sentinel destination, and did not print `Pełna ścieżka raportu:`. This independently verifies the no-false-success-path failure behavior.

## Findings

No Critical, High, Medium, or Low release-blocking findings were identified.

The only verification limitation is environmental: Python 3.10 was not installed, so a native 3.10 execution was not performed. The package metadata, empty dependency declaration, Python 3.10 grammar parse, and successful Python 3.14.7 build/runtime provide supporting evidence; this is not a blocker for this release audit.

## Actions deliberately not performed

- No source, test, configuration, lockfile, service, or infrastructure fix was applied.
- No `git add`, commit, push, tag, reset, restore, or checkout was performed.
- No sudo or system mutation was performed.
- No NeuralEngine write was performed.
- `.codex/` was not read, changed, or used as an output target.

The implementation is ready to commit as v0.1.1. After that commit exists and the normal commit-level checks remain green, it is ready for the v0.1.1 tag.
