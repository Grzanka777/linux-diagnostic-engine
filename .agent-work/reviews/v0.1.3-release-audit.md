# LDE v0.1.3 independent artifact-level release audit

Date: 2026-08-30

## Verdict

The frozen v0.1.3 wheel passed the artifact-level release audit.

V0_1_3_RELEASE_AUDIT = PASS
READY_TO_COMMIT_V0_1_3 = YES
READY_TO_TAG_V0_1_3_AFTER_COMMIT = YES

## Checkpoint and scope

The prerequisite review was verified before this audit:

- `CLI_PRESENTATION_STABILIZATION = PASS`
- `READY_FOR_V0_1_3_RELEASE_AUDIT = YES`
- source: `.agent-work/reviews/v0.1.3-cli-presentation-stabilization.md:181-202`

The audit was read-only with respect to source, tests, configuration, and Git
history. The only repository artifact created by this audit is this review
file.

Initial and final repository state:

- branch: `master`
- HEAD before and after: `883d1decfe587802214127ef667d655490201c08`
- HEAD reflog before and after: `883d1decfe587802214127ef667d655490201c08 commit: release: prepare linux diagnostic engine v0.1.2`
- tracked diff before and after: identical SHA-256
  `256ae8b4c1827db4f51b8e14549cba4f54d3c5a7fbbbafebea42cfb8e7037a04`
- pre-existing dirty tracked and untracked paths were preserved
- no commit, push, tag, reset, clean, or release action was performed

The globally installed launcher was checked before and after:

```
<REDACTED-PATH> --version
Linux Diagnostic Engine 0.1.2
```

GLOBAL_LDE_0_1_2_PRESERVED = YES

## Frozen candidate and one-build evidence

The candidate was made from the current tracked working-tree files with
`git ls-files`; the copy excluded `.git` and all pre-existing untracked
files. It was built in:

```
/tmp/lde-v0.1.3-release-audit.VQ9jgx/candidate-source
```

The one and only wheel build command was:

```
uv build --wheel --out-dir /tmp/lde-v0.1.3-release-audit.VQ9jgx/wheelhouse
```

Build result:

- exit code: 0
- WHEEL_BUILD_COUNT = 1
- exact artifact:
  `linux_diagnostic_engine-0.1.3-py3-none-any.whl`
- exact SHA-256:
  `6ef3005b75fe32b2f343c476d791752d5577f457fa1907f1c696827fe4183f1c`
- wheel type: `py3-none-any`

The wheel was inspected directly as a ZIP. It contains only the three runtime
modules, license metadata, and standard dist-info:

```
constants.py
diagnostic_rules.py
syscheck.py
linux_diagnostic_engine-0.1.3.dist-info/METADATA
linux_diagnostic_engine-0.1.3.dist-info/RECORD
linux_diagnostic_engine-0.1.3.dist-info/WHEEL
linux_diagnostic_engine-0.1.3.dist-info/entry_points.txt
linux_diagnostic_engine-0.1.3.dist-info/licenses/LICENSE
linux_diagnostic_engine-0.1.3.dist-info/top_level.txt
```

Wheel metadata checks passed:

- version: `0.1.3`
- Requires-Python: `>=3.10`
- runtime `Requires-Dist` entries: 0
- console script: `lde = syscheck:main`
- pure-Python wheel: yes
- NO_RUNTIME_DEPENDENCIES = YES

## Fresh virtual environment and installed CLI

A fresh venv was created at:

```
/tmp/lde-v0.1.3-release-audit.VQ9jgx/venv
```

The runtime was Python 3.14.7. An initial `uv pip install` attempt was
blocked by the environment's read-only UV cache. The exact same wheel was then
installed locally with the venv's pip, with `--no-deps`; no alternate wheel
was built and no global environment was changed.

Installed package metadata reported:

- name: `linux-diagnostic-engine`
- version: `0.1.3`
- Requires: empty

Outside the repository, the installed command returned:

```
/tmp/lde-v0.1.3-release-audit.VQ9jgx/venv/bin/lde --version
Linux Diagnostic Engine 0.1.3
```

Root, run, and compare help all exited 0 and were English-only. The run help
exposed `--print-report` and `--verbose`, and no unexpected public
presentation dependency was installed.

## Installed real-machine CLI proofs

All runs below used the exact installed wheel from the fresh venv and were
executed outside the repository. All report and snapshot destinations were
under the disposable audit directory.

| Proof | Result |
| --- | --- |
| default `lde run` | exit 0; 38 output lines; compact summary; report path printed |
| default report dump | absent; `# Linux Diagnostic Engine (LDE)` absent from stdout |
| default internal markers | `Stage 1`, `Stage 2`, `Stage 3`, `RAW`, `OBS`, and `INT` absent |
| default status | `HEALTH STATUS    ✓ HEALTHY`; 0 findings and 0 recommendations |
| `--verbose` | exit 0; detailed English progress on stderr; no report dump |
| `--print-report` | exit 0; marker count 1; report body byte-equal to saved report; heading count 1 |
| `--verbose --print-report` | exit 0; verbose progress and one complete report |
| TTY with color enabled | exit 0; exactly two ANSI SGR codes around `HEALTHY` |
| TTY with `NO_COLOR=` | exit 0; zero ANSI codes |
| piped with `NO_COLOR` removed | exit 0; zero ANSI codes |
| `COLUMNS=1` | exit 0; safe one-character rule fallback; no crash |
| `XDG_DATA_HOME=<audit>/runtime/xdg` | exit 0; report under `<audit>/runtime/xdg/lde/reports` |

The default, verbose, printed-report, and piped outputs were captured and
checked for ANSI, forbidden internal markers, Markdown dump behavior, English
public labels, and a single deterministic status line.

The three-way severity mapping was also exercised by the existing public
behavior tests: no actionable severity gives HEALTHY, P2/P3 gives ATTENTION,
and P0/P1 gives PROBLEMS. No threshold, rule, evidence, or diagnostic
collection logic was changed by this audit.

## Snapshot and report compatibility

Two snapshots generated by the installed exact wheel passed:

- `schema_version = 3`
- `metadata.syscheck_version = 2.1.0`
- report contains product version `0.1.3`
- report contains compatibility `2.1.0`

`lde compare` over those two snapshots exited 0 and generated an English
comparison report with no significant changes.

For diagnostic-behavior regression evidence, the globally installed 0.1.2
launcher was run separately into the disposable audit directory. After
removing only the two run timestamps, the candidate snapshot and the 0.1.2
snapshot were exactly equal. This included top-level structure, execution
counts, restrictions, recommendations, observations, findings, and all
captured diagnostic data available in the snapshots:

- commands: 69
- findings: 0
- recommendations: 0
- restrictions: 5
- normalized snapshot equality: yes

DIAGNOSTIC_BEHAVIOR_UNCHANGED = YES
SNAPSHOT_SCHEMA_3 = YES
REPORT_COMPATIBILITY_2_1_0 = YES

## Destination, read-only, and privacy checks

The installed exact wheel was exercised against existing destinations:

- existing compare output was rejected with exit 1 and preserved byte-for-byte
- existing snapshot destination was rejected with exit 1 and preserved
  byte-for-byte
- neither failure emitted a traceback
- all diagnostic outputs were written only below the disposable audit root or
  the explicitly supplied XDG destination

The exact wheel source and captured public outputs were checked for AI/scoring
identity and secret-bearing public markers. No matches were found for the
relevant markers, including `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`,
`BINGX_SECRET_KEY`, `Authorization: Bearer`, `**Internal metadata:**`,
`**Internal metadata:**`, or the prior AI identity strings.

READ_ONLY_DESTINATIONS = PASS
PUBLIC_PRIVACY_CHECK = PASS
NO_AI_OR_SCORING = YES

## Full repository validation

Commands run after artifact proofs:

```
env -u PYTEST_ADDOPTS TMPDIR=/tmp TMP=/tmp TEMP=/tmp PYTHONDONTWRITEBYTECODE=1 python -m pytest -q
896 passed in 6.17s

ruff check .
All checks passed!

ruff format --check .
8 files already formatted

git diff --check
exit 0
```

FULL_PYTEST = PASS
RUFF_CHECK = PASS
RUFF_FORMAT_CHECK = PASS
GIT_DIFF_CHECK = PASS

## Uninstall and final Git proof

The exact installed package was uninstalled from the disposable venv:

- uninstall exit code: 0
- venv `lde` launcher absent after uninstall
- `pip show linux-diagnostic-engine` failed after uninstall, as expected
- global 0.1.2 launcher remained present and unchanged

The tracked diff hash before and after the complete audit was identical, and
HEAD, branch, and reflog were unchanged.

SOURCE_UNMODIFIED_DURING_AUDIT = YES
DISPOSABLE_EXACT_WHEEL_UNINSTALLED = YES
GIT_HISTORY_UNCHANGED = YES

## Release gate summary

| Gate | Result |
| --- | --- |
| installed version 0.1.3 | PASS |
| compact default and no default Markdown dump | PASS |
| `--print-report` prints exactly once | PASS |
| `--verbose` without report dump | PASS |
| English public CLI | PASS |
| internal Stage/RAW/OBS/INT markers hidden by default | PASS |
| deterministic HEALTHY/ATTENTION/PROBLEMS mapping | PASS |
| restrained ANSI only on TTY | PASS |
| `NO_COLOR` respected | PASS |
| no ANSI when piped | PASS |
| graceful terminal-width handling | PASS |
| report compatibility 2.1.0 | PASS |
| snapshot schema 3 | PASS |
| diagnostic behavior unchanged | PASS |
| no runtime dependencies | PASS |
| no source modification during audit | PASS |
| exact report path and XDG output behavior | PASS |
| destination/read-only/privacy checks | PASS |
| no global 0.1.2 overwrite | PASS |
| full pytest and Ruff | PASS |
| uninstall completed | PASS |
| Git unchanged apart from this requested review artifact | PASS |

No material unresolved findings remain for the stated v0.1.3 artifact audit.
