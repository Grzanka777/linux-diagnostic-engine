# Linux Diagnostic Engine v0.2.0 — candidate assessment and release review

Date: 2026-08-31
Verdict: PASS — candidate is compatible and ready for the authorized release sequence.

## 1. Checkpoint

- Repository: `/run/media/<USER>/<VOLUME>/projekty/linux-diagnostic-engine`
- Branch: `master`
- Pre-release HEAD: `5c6951756e4edd0e1888028b57b2cff799c784d3`
- Pre-release `origin/master`: `5c6951756e4edd0e1888028b57b2cff799c784d3`
- Pre-release tag at HEAD: `v0.1.5`
- Baseline product/report/schema: `0.1.5 / 2.1.0 / 3`
- NeuralEngine status: `TRUSTED_CURRENT`; no Brain write performed.
- Pre-release staged paths: none.
- Pre-existing unrelated untracked paths preserved: `.codex/`, `build/`, `linux_diagnostic_engine.egg-info/`, and prior release-result reviews.

## 2. Exact diff

The tracked candidate diff before this review file was created was limited to:

| Path | Added | Removed | Hunk summary |
| --- | ---: | ---: | --- |
| `README.md` | 3 | 3 | Update wheel and public product version documentation to 0.2.0. |
| `constants.py` | 1 | 1 | Bump `PRODUCT_VERSION`; preserve report compatibility metadata. |
| `diagnostic_rules.py` | 17 | 0 | Add the evidence-only user-systemd source-failure rule and register it. |
| `pyproject.toml` | 1 | 1 | Bump distribution version to 0.2.0. |
| `syscheck.py` | 109 | 3 | Add bounded source-failure capture, classification, evidence, observation, restriction, and internal accounting route; update header. |
| `test_packaging.py` | 4 | 4 | Update packaging/version assertions. |
| `test_syscheck.py` | 173 | 6 | Add user-systemd state tests and update empty-query fixtures to authoritative zero output. |

Diff stat: `308 insertions(+), 18 deletions(-)` across 7 tracked paths.

Full tracked diff SHA-256: `9e68100d7f6816a226789949bdc225670f3654f1734b74db690dc1bc2e02ad1f`.

No new collector, CLI command/flag, runtime dependency, session/graphics diagnostic, report section, persisted snapshot key, or schema migration was introduced.

## 3. Scope review

`V0_2_0_SCOPE_REVIEW = PASS`.

The implementation distinguishes:

```text
systemctl --user --failed source failure
!= authoritative zero
!= actual failed user unit
```

The accepted route is: explicit `RawDiagnostic` → explicit `Observation` → direct/partial `Evidence` → deterministic pipeline rejection → public restriction/presentation, with no failed-user-service `Finding`. Existing actual failed-user-unit parsing and semantics remain unchanged. Existing finding IDs, severities, confidences, recommendation intents, and recommendations were unchanged in the real-machine comparison.

## 4. Report compatibility assessment

`REPORT_COMPATIBILITY_2_1_0_SAFE = YES`.

Report-facing differences are additive and non-breaking:

- `ADDITIVE_NON_BREAKING`: one restriction line containing source status, return code, and captured stderr;
- `PRESENTATION_ONLY`: one explanatory note that a user-service failure is not treated as a failed service;
- `PRESENTATION_ONLY`: source failure remains visible in the existing systemd command output.

The baseline and post-fix reports retain 66 identical headings in identical order and 22 identical bold field labels. Product and compatibility metadata retain their existing labels. No heading or existing Finding/recommendation field was added, removed, renamed, retyped, or given a new meaning. The new source-failure ID is absent from the confirmed-finding section.

## 5. Snapshot schema assessment

`SNAPSHOT_SCHEMA_3_SAFE = YES`.

The existing schema-3 top-level keys remain exactly:

```text
environment, evidence, execution, findings, metadata, observations,
raw_diagnostics, recommendations, restrictions, schema_version
```

`RawDiagnostic`, `Observation`, and `Evidence` use existing serialized collections and existing field shapes. `pipeline_accounting` remains runtime-only and is absent from JSON. `SNAPSHOT_SCHEMA_VERSION` remains `3`; the migrator is unchanged and schema-3 reading remains supported. The new source-failure observation is intentionally incomplete and does not enter `environment.failed_units`.

## 6. Public type/enum assessment

`PUBLIC_TYPE_CONTRACT_SAFE = YES`.

- New raw ID: `SYSD-USR-SOURCE-FAIL-001`; existing raw IDs and categories are preserved.
- New observation ID/category: `SYSD-USR-SOURCE-FAIL-001` / `systemd_user_source_failure`.
- New evidence ID pattern: `EVIDENCE-SYSD-USR-SOURCE-FAIL-001-001`; evidence type is the existing `command_result`.
- `FindingKind.SOURCE_FAILURE` and its classification are additive internal routing values; no persisted Finding uses them for the source-failure path.
- No existing enum value, dataclass field, serialized value, stable Finding ID, severity, confidence, or recommendation value changed.
- Existing `DiagnosticDomain`, `Actionability`, `RecommendationIntent`, observation, evidence, raw-diagnostic, finding, recommendation, and execution field types remain compatible.

## 7. Old/new snapshot compatibility proof

Compared with the candidate code:

- old: `/tmp/lde-v015-session-baseline-rerun.sXfsBz/snapshot.json`;
- new: `/tmp/lde-v015-session-final.2PAjFA/snapshot.json`.

Both loaded through `SystemSnapshot.from_json`, returned `validate() == []`, and had schema `3`. Top-level key sets and top-level value types were equal. Counts changed only additively from `raw/obs/evidence/findings/recommendations/restrictions = 5/5/4/4/4/6` to `6/6/5/4/4/7`. The new snapshot contained the source-failure raw, observation, and evidence IDs listed above. Finding IDs and recommendation payloads were equal. `SnapshotComparator.compare` completed with `has_changes=False`, no new/resolved/changed Finding, and no environment changes. `pipeline_accounting` was not serialized.

## 8. Focused tests

- User-systemd source contract: `8 passed, 893 deselected`.
- Snapshot-focused tests: `46 passed, 855 deselected`.
- Packaging/CLI contract: `9 passed`.
- Destination safety is covered by existing exclusive-write tests for files, symlinks, dangling symlinks, and compare outputs.

## 9. Full validation

After the 0.2.0 bump:

```text
FULL_TEST_SUITE = PASS
FULL_TEST_COUNT = 910
ruff format --check . = PASS (8 files already formatted)
ruff check . = PASS
git diff --check = PASS
python3 syscheck.py --version = Linux Diagnostic Engine 0.2.0
```

The full command was run with `PYTEST_ADDOPTS` unset and temporary paths under `/tmp`.

## 10. Real-machine proof

The post-bump checkout run used the real CachyOS workstation and completed RC 0:

- evidence path: `/tmp/lde-v020-final-source.EzJs2u/`;
- 69 commands, 6 raw diagnostics, 6 observations, 5 evidence objects;
- 4 existing Findings and 4 existing recommendations;
- 7 restrictions;
- product `0.2.0`, report compatibility `2.1.0`, schema `3`;
- source failure preserved as `status=error`, `rc=1`, `authoritative=false`, with stderr captured;
- no `SYSD-USR-FAIL-001` false positive, no traceback.

The pre-bump real-machine run independently produced the same bounded contract and RC 0.

## 11. Version bump

Only normal product-version surfaces were changed:

- `constants.py`: `PRODUCT_VERSION = "0.2.0"`;
- `pyproject.toml`: distribution version `0.2.0`;
- `syscheck.py`: product header `0.2.0`;
- `README.md`: wheel filename and public version documentation;
- version assertions in `test_packaging.py` and report assertions in `test_syscheck.py`.

Preserved values: `REPORT_COMPATIBILITY_VERSION = "2.1.0"`, `SCRIPT_VERSION = "2.1.0"`, and `SNAPSHOT_SCHEMA_VERSION = 3`.

## 12. Exact wheel filename/path/SHA

- Build output directory: `/tmp/lde-v020-artifact.9Qb5wy/wheelhouse`.
- Certified wheel: `linux_diagnostic_engine-0.2.0-py3-none-any.whl`.
- Exact path: `/tmp/lde-v020-artifact.9Qb5wy/wheelhouse/linux_diagnostic_engine-0.2.0-py3-none-any.whl`.
- SHA-256: `b65f5ca37b4b8130552c0408b58efdd1e50e300f0f7c46c5e8d00dfb08a8fde7`.
- Successful wheel build count: one.
- No rebuild after certification.

The first `uv build` attempt created no artifact because the default cache was read-only; the successful build used a cache directory under `/tmp` and the certified wheel was built once.

## 13. Wheel content audit

Wheel metadata confirmed:

- distribution `linux-diagnostic-engine`, version `0.2.0`;
- MIT license;
- `Requires-Python: >=3.10`;
- no `Requires-Dist` runtime dependencies;
- entry point `lde = syscheck:main`;
- pure Python `py3-none-any` wheel.

Members were only `constants.py`, `diagnostic_rules.py`, `syscheck.py`, and expected `.dist-info` metadata/license files. No credentials, private keys, email addresses, project-local paths, AI/provider identity, or unexpected files were present. The only broad token check hit was the existing literal `localhost` in a diagnostic message.

## 14. Isolated install/runtime proof

The exact certified wheel installed successfully with `--no-deps --no-index` into `/tmp/lde-v020-artifact.9Qb5wy/venv`, outside the checkout. From that environment:

- `lde --version`: RC 0, `Linux Diagnostic Engine 0.2.0`;
- `lde --help`: RC 0;
- `lde run --help`: RC 0;
- `lde compare --help`: RC 0;
- real run: RC 0, report and snapshot under `/tmp/lde-v020-artifact-run.hPHJhy/`;
- installed distribution metadata: version `0.2.0`;
- artifact runtime output preserved compatibility `2.1.0`, schema `3`, the source-failure evidence, and no false user-service Finding.

## 15. Compatibility/schema proof

The exact artifact run loaded successfully with `SystemSnapshot.from_json`, `validate() == []`, and schema `3`. It produced the same `69/6/6/5/4/4/7` command/raw/observation/evidence/finding/recommendation/restriction contract as the validated checkout run. Report metadata was product `0.2.0` and compatibility `2.1.0`. A second artifact run targeting the existing snapshot stopped at the exclusive destination guard with RC 1 and `Destination .../snapshot.json already exists`; it did not overwrite the destination and emitted no traceback. The temporary environment uninstall removed the `lde` entry point and package metadata.

## 16. Git verification

Before release commit, the release guard must re-fetch and require `origin/master` to remain exactly `5c6951756e4edd0e1888028b57b2cff799c784d3`. Authorized release paths are:

```text
README.md
constants.py
diagnostic_rules.py
pyproject.toml
syscheck.py
test_packaging.py
test_syscheck.py
.agent-work/reviews/post-v0.1.5-session-service-reliability-pack.md
.agent-work/reviews/v0.2.0-candidate-assessment-and-release.md
```

The existing `.codex/`, `build/`, `linux_diagnostic_engine.egg-info/`, and prior release-result reviews remain out of scope. No history rewrite, amend, rebase, reset, force push, or tag movement is authorized. The post-v0.1.5 reliability review is included in the authorized release content.

## 17. Blockers/issues

- Release blockers: `0`.
- High issues: `0`.
- One environmental deviation: the default uv cache was read-only; redirecting cache to `/tmp` resolved it without changing source or artifact semantics.
- No compatibility, schema, type, test, lint, runtime, artifact, or scope deviation remains.

## 18. Release verdict

```text
V0_2_0_SCOPE_REVIEW = PASS
REPORT_COMPATIBILITY_2_1_0_SAFE = YES
SNAPSHOT_SCHEMA_3_SAFE = YES
PUBLIC_TYPE_CONTRACT_SAFE = YES
V0_2_0_COMPATIBILITY_GATE = PASS
READY_TO_COMMIT_V0_2_0 = YES
```

The candidate is safe to release as Linux Diagnostic Engine v0.2.0. Continue only through the prompt-authorized single commit, remote-baseline guard, normal master/tag pushes, fresh public HTTPS clone verification, and local-only release-result review.
