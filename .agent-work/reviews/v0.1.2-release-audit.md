# LDE v0.1.2 Independent Artifact-Level Release Audit

## 1. Checkpoint

- Branch: `master`.
- Starting and final HEAD:
  `2c711739c16bc21668c960268d5df8725ec5b64c`.
- Starting staged diff: empty.
- The requested mount `<REDACTED-PATH>` intermittently reported I/O
  errors. The same `LABEL=777` volume was audited through
  `<REDACTED-PATH>`.
- Pre-existing untracked paths were preserved: `.codex/`, `build/`, and
  `linux_diagnostic_engine.egg-info/`.
- `neural status` was attempted but the `neural` command is unavailable.
  No NeuralEngine Brain write was performed.

## 2. Exact candidate and one build

- Candidate source was created in a disposable `/tmp` directory from
  `git archive HEAD` plus the exact current tracked worktree diff. Untracked
  build/cache/review artifacts were excluded.
- Candidate source:
  `/tmp/lde-v0.1.2-release-audit.Gr2QMe/candidate-source`.
- Builder: `uv 0.5.9`; Python `3.14.7`.
- Build command, executed once:
  `uv build --wheel --out-dir /tmp/lde-v0.1.2-release-audit.Gr2QMe/dist`.
- Certified wheel:
  `linux_diagnostic_engine-0.1.2-py3-none-any.whl`.
- Exact wheel:
  `/tmp/lde-v0.1.2-release-audit.Gr2QMe/dist/linux_diagnostic_engine-0.1.2-py3-none-any.whl`.
- SHA-256:
  `14383179b3f3b4597b8eb373f45f021ca24d287ba0a245fb7a3f65fc3f1bc073`.
- Size: `65402` bytes.

## 3. Exact wheel inspection

The wheel contained exactly these files:

```text
constants.py
diagnostic_rules.py
syscheck.py
linux_diagnostic_engine-0.1.2.dist-info/licenses/LICENSE
linux_diagnostic_engine-0.1.2.dist-info/METADATA
linux_diagnostic_engine-0.1.2.dist-info/WHEEL
linux_diagnostic_engine-0.1.2.dist-info/entry_points.txt
linux_diagnostic_engine-0.1.2.dist-info/top_level.txt
linux_diagnostic_engine-0.1.2.dist-info/RECORD
```

- Exact expected file set: pass.
- No `.env`, key, token, credential, dump, backup, bytecode, or cache files:
  pass.
- `METADATA`: name `linux-diagnostic-engine`, version `0.1.2`, Python
  requirement `>=3.10`: pass.
- No `Requires-Dist` entries: pass.
- Entry point `lde = syscheck:main`: pass.
- Wheel tag `py3-none-any`: pass.
- LICENSE is packaged under dist-info and matches candidate source: pass.
- All non-RECORD hashes and sizes validate against `RECORD`: pass.
- Wheel-wide stale identity scan for `Agent`, `Model`,
  `<REDACTED-ROLE>`, `<REDACTED-PROVIDER>`, `<REDACTED-AI-LABEL>`, and `<REDACTED-AI-TERM>`: pass.
- Wheel-wide high-confidence secret marker scan: pass.

The packaged `syscheck.py` filename and entry-point target are retained as the
documented flat-module compatibility seam; public CLI/report/compare output
was audited separately for stale SysCheck branding.

## 4. Isolated exact-wheel installation

- Fresh venv:
  `/tmp/lde-v0.1.2-release-audit.Gr2QMe/venv`.
- Install used exactly the certified wheel with `--no-index --no-deps`.
- Valid outside-repo metadata probe reported:
  `linux-diagnostic-engine==0.1.2`, no runtime dependencies, exact `lde`
  console entry point, and module loaded from venv `site-packages`.
- `pip list` contained only the package and venv bootstrap `pip` before
  uninstall.

An initial metadata probe from the repository cwd was rejected because it
resolved the pre-existing repository `linux_diagnostic_engine.egg-info`
(`0.1.1`). The required outside-repo probe then passed; no reinstall or
second build was needed.

## 5. Public surface and real-machine proof

All commands below used the installed wheel from a temporary cwd outside the
repository and unset inherited `PYTHONPATH`.

- `lde --version` -> `Linux Diagnostic Engine 0.1.2`: pass.
- `lde --help` contains the LDE identity, no stale public `syscheck` branding,
  and no stale AI identity: pass.
- Real-machine run exit status: `0`.
- Exact report path printed once:
  `/tmp/lde-v0.1.2-real-installed-report.k6B1xY/lde-g777-20260830-095014.md`.
- Absolute path, file creation, and no traceback: pass.
- Report contains product `Linux Diagnostic Engine (LDE)`, version `0.1.2`,
  and compatibility `2.1.0`: pass.
- Report contains no `**Internal metadata:**`, `**Internal metadata:**`,
  `<REDACTED-ROLE>`, `<REDACTED-PROVIDER>`, `przez <REDACTED-ROLE>`, or
  `<REDACTED-AI-LABEL>`: pass.
- Report contains no stale public SysCheck branding: pass.

The generic `<REDACTED-AI-TERM>` scan had one legitimate host-data match: `LLMNR` in
`resolvectl status`, not an AI identity. The report also exposed the literal
host service variable name `BINGX_SECRET_KEY` in a systemd command line, but
no secret value, bearer value, token prefix, private key, or assignment value
was present. This is host diagnostic data, not a packaged secret.

## 6. Snapshot and compare

- Snapshot one: `/tmp/lde-v0.1.2-snapshot-one.UcKw23/snapshot.json`.
- Snapshot two: `/tmp/lde-v0.1.2-snapshot-two.0CoUyJ/snapshot.json`.
- Both installed-wheel runs exited `0` and created files.
- Both snapshots have schema `3`, compatibility `2.1.0`, and no stale
  identity markers: pass.
- `lde compare` exited `0` and wrote:
  `/tmp/lde-v0.1.2-release-audit.Gr2QMe/compare.md`.
- Compare output is non-empty and has no stale identity or public SysCheck
  branding: pass.

## 7. Safety, read-only behavior, and privacy

- Real report recorded 69 executed diagnostic commands.
- Command-list scan found no `sudo`, network client, mutating `systemctl`,
  package mutation, or filesystem mutation command.
- Source has no HTTP client API or secret-value assignment.
- Optional `psutil` import has an ImportError fallback; installed wheel runs
  without `psutil`, and the wheel declares no dependency for it.
- No credential values were found in the real report or wheel. Report/snapshot/
  compare files were the only expected writes, all to explicit temporary paths.

## 8. Full validation

- Clean candidate full suite:
  `env -u PYTEST_ADDOPTS TMPDIR=/tmp TMP=/tmp TEMP=/tmp
  PYTHONDONTWRITEBYTECODE=1 python -m pytest -q` -> `883 passed in 9.10s`.
- Clean candidate `ruff format --check .` -> `8 files already formatted`.
- Clean candidate `ruff check .` -> `All checks passed!`.
- Repository `git diff --check` -> pass with no output.

## 9. Uninstall and exact-candidate recheck

- SHA-256 immediately before uninstall remained
  `14383179b3f3b4597b8eb373f45f021ca24d287ba0a245fb7a3f65fc3f1bc073`.
- `pip uninstall --yes linux-diagnostic-engine`: pass.
- Post-uninstall package, `lde` console script, module import, and pip-list
  presence checks: pass.

## 10. Git and scope verification

- HEAD, branch, and reflog remained unchanged.
- No commit, push, tag, reset, or history mutation occurred.
- No diagnostic rule, threshold, schema, compatibility version, AI
  integration, telemetry, network integration, sudo behavior, or packaging
  redesign was added.
- The only tracked product changes remain the v0.1.2 identity/version cleanup
  and its regression tests; this review is the requested audit artifact.

## 11. Known limitations

- Live Python 3.10 execution was not available; manifest compatibility is
  `>=3.10` and the certified runtime proof used Python 3.14.7.
- `neural` CLI was unavailable; Brain state was not modified.
- The canonical mount had intermittent I/O errors; the same volume was used
  through `<REDACTED-PATH>`.
- Historical backups and generated host reports outside the wheel retain old
  identity text by design and were not deleted.

## 12. Verdict

```text
V0_1_2_RELEASE_AUDIT = PASS
READY_TO_COMMIT_V0_1_2 = YES
READY_TO_TAG_V0_1_2_AFTER_COMMIT = YES
```
